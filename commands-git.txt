pour cloner :créer un nouveau repository : git init
puis via terminal : git clone +le lien du repository



pour push sur github
git add .
git commit -m "message"
git push 

git pull origin main

creer branche : git branch NomDeLaBranche

git checkout